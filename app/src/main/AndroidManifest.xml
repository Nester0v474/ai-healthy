<?xml version="1.0" encoding="utf-8"?>
<!-- Манифест приложения: разрешения, активности -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Мне нужен интернет для чата с backend -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Для напоминаний — уведомления и будильник -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AiHealthy"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        tools:replace="android:usesCleartextTraffic">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AiHealthy"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.foodtracker.FoodTrackerActivity"
            android:exported="false"
            android:label="Трекер питания"
            android:theme="@style/Theme.AiHealthy" />
        <activity
            android:name=".ui.MainMenuActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.AiHealthy" />
        <activity
            android:name=".ui.moodtracker.MoodTrackerActivity"
            android:exported="false"
            android:label="Дневник настроения"
            android:theme="@style/Theme.AiHealthy" />
        <activity
            android:name=".ui.habittracker.HabitTrackerActivity"
            android:exported="false"
            android:label="Трекер привычек"
            android:theme="@style/Theme.AiHealthy" />
        <activity
            android:name=".ui.reminders.RemindersActivity"
            android:exported="false"
            android:label="Напоминания"
            android:theme="@style/Theme.AiHealthy" />
        <activity
            android:name=".ui.sleepdiary.SleepDiaryActivity"
            android:exported="false"
            android:label="Дневник сна"
            android:theme="@style/Theme.AiHealthy" />
        <activity
            android:name=".ui.healthytasktracker.HealthyTaskTrackerActivity"
            android:exported="false"
            android:label="Ежедневник"
            android:theme="@style/Theme.AiHealthy" />
        <activity
            android:name=".ui.healthy.HealthyActivity"
            android:exported="false"
            android:label="Healthy"
            android:theme="@style/Theme.AiHealthy"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <receiver
            android:name=".reminders.ReminderReceiver"
            android:exported="false" />
    </application>

</manifest>