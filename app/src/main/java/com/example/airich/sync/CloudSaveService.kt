package com.example.airich.sync

/**
 * Временный файл-заглушка.
 *
 * Ранее здесь была попытка интеграции Cloud Save через
 * Google Play Games (Snapshots API), но из-за несовместимости
 * SDK это вызывало ошибки сборки.
 *
 * Чтобы проект стабильно собирался, синхронизацию через облако
 * мы временно отключили и оставили только локальное хранение
 * подписки и чата (Room + SharedPreferences).
 *
 * Этот файл оставлен пустым, чтобы в будущем можно было
 * безопасно добавить рабочую реализацию Cloud Sync.
 */
class CloudSaveService
