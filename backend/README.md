# Ai Healthy Backend

Я — Node.js сервер, проксирую запросы к DeepSeek API.

## Локально
```bash
npm install
# Создай .env с DEEPSEEK_API_KEY и PORT=3000
npm start
```

## VPS
Скопируй файлы на сервер и запусти `install-on-server.sh` (создаёт файлы в `/root/backend`).
